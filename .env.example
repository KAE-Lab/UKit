# Variables d'environnement de UKit.
#
# Copier en `.env` (ignore par git) et renseigner. Les memes noms servent de secrets EAS pour les
# builds. Voir docs/backend.md.

# Rapport d'erreurs.
SENTRY_DSN=

# La base de publication (Blueprints, annonces, referentiels, catalogue des etablissements).
# La cle `anon` est publique par conception : elle est lisible dans n'importe quel binaire, et la
# frontiere de securite ce sont les politiques RLS (supabase/policies.sql).
SUPABASE_URL=
SUPABASE_ANON_KEY=

# La livraison des Blueprints. `false` fait ignorer durablement la surcouche publiee : l'application
# ne joue plus que son socle embarque. C'est le troisieme interrupteur d'arret (docs/blueprints.md),
# le seul qui se pose a la construction du binaire. Absent, la livraison est active.
# Attention : app.config.ts lit l'environnement a la construction de la configuration, pas a
# l'execution — changer cette valeur demande `npx expo start -c`, pas un rechargement.
# BLUEPRINTS_REMOTE=false

# Les deux valeurs qui suivent ne vont JAMAIS dans l'application : ni dans `extra`, ni dans un fichier
# versionne, ni dans un secret EAS. Elles vivent sur le poste du publieur et dans les secrets de CI.
# Qui les detient peut ecrire ce que tous les appareils liront.
#
# `service_role` : le script de publication des Blueprints (npm run blueprints:publish) et l'import
# de contenu (npm run content:import).
# SUPABASE_SERVICE_ROLE_KEY=
#
# Le mot de passe de la base : appliquer supabase/schema.sql, fonctions.sql et policies.sql avec psql.
# Voir supabase/README.md.
# SUPABASE_DB_PASSWORD=
#
# Le mot de passe du compte editeur de la console, le temps de le creer ou de le remplacer par
# `npm run console:editeur -- --email <adresse>` (cle service_role requise). Jamais versionne.
# CONSOLE_MOT_DE_PASSE=

# Les identifiants de test des portails universitaires, un couple par etablissement du catalogue
# (docs/phase-6/6-g-etablissements.md). Ils ne vont JAMAIS dans l'application : ils servent a jouer un
# Blueprint de portail depuis le poste et a verifier un parcours sur appareil.
#
#   aetherius run blueprints/portails/ukit-portail-bordeaux-inp-dossier.blueprint.json \
#     --secret portail_user="$PORTAIL_BORDEAUX_INP_USER" \
#     --secret portail_pass="$PORTAIL_BORDEAUX_INP_PASS"
#
# Un seul couple peut porter les noms `portail_user` / `portail_pass` a la fois : c'est pourquoi ils
# sont nommes par etablissement ici et passes explicitement a l'appel, plutot que resolus par
# l'environnement.
# PORTAIL_BORDEAUX_USER=
# PORTAIL_BORDEAUX_PASS=
# PORTAIL_BORDEAUX_INP_USER=
# PORTAIL_BORDEAUX_INP_PASS=
